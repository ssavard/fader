<% provide(:title, 'New Group') %>
<h1>New Group</h1>  
  
<div class="row">
  <div class="span6 offset3">
    <%= form_for(@group) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Create group", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>